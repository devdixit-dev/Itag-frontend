Options -MultiViews
RewriteEngine On
RewriteBase /

# Serve index.html for all routes except real files or folders
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.html [L]